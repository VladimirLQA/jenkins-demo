pipeline {
    agent { label 'test' }

    stages {
        stage('Init') {
            steps {
                sh '''
                    echo "Initializing..."
                '''
            }
        }
    }
}